apiVersion: v1
kind: Service
metadata:
  labels:
    app: relay-svc
  name: relay-svc
  namespace: rootenv-infra
spec:
  ports:
  - name: 8080-8080
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: relay
  type: ClusterIP
status:
  loadBalancer: {}
