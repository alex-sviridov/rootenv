apiVersion: apps/v1
kind: Deployment
metadata:
  name: contmgr
  namespace: rootenv-infra
  labels:
    app: contmgr
spec:
  replicas: 1
  selector:
    matchLabels:
      app: contmgr
  template:
    metadata:
      labels:
        app: contmgr
    spec:
      serviceAccountName: contmgr
      containers:
        - name: contmgr
          image: rootenv-registry:5000/rootenv-contmgr
          imagePullPolicy: IfNotPresent
          securityContext:
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            seccompProfile:
              type: RuntimeDefault
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          ports:
            - containerPort: 8081
              name: health
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
            periodSeconds: 10
          env:
            - name: CONTMGR_BACKEND_URL
              value: http://backend-svc:8090
            - name: CONTMGR_POLL_INTERVAL
              value: "5s"
            - name: CONTMGR_IMAGE_REGISTRY
              value: "rootenv-registry:5000"
          envFrom:
            - secretRef:
                name: contmgr-secrets
