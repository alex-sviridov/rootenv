apiVersion: k3d.io/v1alpha5
kind: Simple
metadata:
  name: rootenv
servers: 1
agents: 0
ports:
  - port: "8080:80"
    nodeFilters:
      - loadbalancer
options:
  k3s:
    extraArgs:
      - arg: --tls-san=0.0.0.0
        nodeFilters:
          - server:*
